/* ═══ Input Content — Client Component with TransactionInput ═══ */
'use client';

import TransactionInput from '@/components/ui/TransactionInput';

export default function InputContent() {
  return (
    <div className="stack stack-md">
      <TransactionInput />
    </div>
  );
}